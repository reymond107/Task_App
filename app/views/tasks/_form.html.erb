<%= form_with(model: task, url: create_task_path, local: true) do |form| %>
<%# <%= form_with scope: :task, url: update_task_path, local: true, method: :patch do |f|  %> 
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
        <% task.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  <div >
      <%= form.label :name %>
      <%= form.text_field :name %>
  </div><br>

  <div >
      <%= form.label :date %>
      <%= form.date_field :date,  :class  => 'date-field' %>
  </div><br>

  <div >
      <%= form.label :description %>
      <%= form.text_area :description, :class  => 'text-field' %>
  </div><br>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
